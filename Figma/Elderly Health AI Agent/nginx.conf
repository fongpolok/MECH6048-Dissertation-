server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback: let the React router (if any) / client-side app handle unknown paths.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
